
import java.util.*;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;



public class DiceTest {

    // private Dice dice;

    // @Before
    // public void setup() {
    // dice = new Dice(); // Initialise new Dice
    // }

    // @Test
    // public void testRollProducesValidNumbers() {
    // dice.roll();
    // int die1 = dice.getDie1();
    // int die2 = dice.getDie2();

    // assertTrue("Die 1 should be between 1 and 6", die1 >= 1 && die1 <= 6);
    // assertTrue("Die 2 should be between 1 and 6", die2 >= 1 && die2 <= 6);
    // }

    // @Test
    // public void testIsDouble() {
    // dice.roll();

    // assertTrue("Dice should return true for doubles", dice.isDouble());
    // assertFalse("Dice should return false for non-doubles", dice.isDouble());
    // }

    // @Test
    // public void testGetMovesRegularRoll() {
    // // Simulate a regular roll
    // dice.roll();
    // int die1 = dice.getDie1();
    // int die2 = dice.getDie2();

    // ArrayList<Integer> moves = dice.getMoves();
    // assertEquals("Regular roll should produce 2 moves", 2, moves.size());
    // assertEquals("First move should match first die value", die1, (int)
    // moves.get(0));
    // assertEquals("Second move should match second die value", die2, (int)
    // moves.get(1));
    // }

}
